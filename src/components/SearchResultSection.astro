---
import UnderlineText from "./UnderlineText.astro";
import SelectChip from "./SelectChip.astro";
import SongCard from "./SongCard.astro";

import type { Song } from "../data/index";
interface Option {
    label: string;
    value: string;
}

interface Props {
    sortedSongs: Song[];
    sortOptions: Option[];
    currentSort: string;
}

const { sortedSongs, sortOptions, currentSort } = Astro.props;
---

<div class="search-result-section">
    <UnderlineText>全 {sortedSongs.length} 曲</UnderlineText>
    <SelectChip 
        name="sort" 
        options={sortOptions} 
        defaultValue={currentSort}
        submitOnChange={true}
    />
</div>

<style>
    .search-result-section {
        display: flex;
        flex-direction: column;
    }
</style>